# Повторный поиск Email с DeepSeek

## Дата: 29 ноября 2025

## Проблема

После обработки Stage 3 (поиск email) некоторые компании остаются без email (`stage3_status = 'failed'`). Perplexity Sonar не всегда может найти контактную информацию на китайских сайтах.

## Решение

Добавлен **Stage 3 Retry** - повторный поиск email используя **DeepSeek Chat** с более агрессивным промптом.

## Файлы

### 1. `src/stages/Stage3Retry.js`
Новый класс для повторного поиска email:

**Логика:**
- Выбирает компании с `stage3_status = 'failed'`
- Имеют сайт, но НЕ имеют email
- `current_stage >= 2` (прошли Stage 2)

**Особенности:**
- Использует DeepSeek Chat (более дешевая модель)
- Более агрессивный промпт с четкими инструкциями
- Просит вернуть JSON с email, source, confidence
- Валидирует email перед сохранением
- Обрабатывает последовательно с паузой 2 секунды

**Процесс:**
1. Получить список компаний для retry
2. Для каждой компании:
   - Создать промпт с названием, сайтом, описанием
   - Отправить в DeepSeek Chat
   - Парсить JSON ответ
   - Валидировать email
   - Сохранить результат в БД
3. Обновить `stage3_status` на 'completed' если найден email
4. Сохранить `stage3_raw_data` с пометкой `source: 'deepseek_retry'`

### 2. `src/api/sessions.js`
Добавлен новый endpoint:

```javascript
POST /api/sessions/retry-email-search
```

**Ответ:**
```json
{
  "success": true,
  "total": 10,     // Всего компаний обработано
  "found": 5       // Email найдено
}
```

### 3. `public/index.html`
Добавлена кнопка и UI:

**Кнопка:** 
```html
🔄 Повторный поиск email (DeepSeek)
```

**Статус блок:**
- Показывает прогресс обработки
- Отображает результаты: обработано / найдено / не найдено
- Автообновление статистики после завершения

## Как использовать

### Шаг 1: Перейти на главную страницу
```
http://localhost:3030
```

### Шаг 2: Нажать кнопку
```
🔄 Повторный поиск email (DeepSeek)
```

### Шаг 3: Подтвердить
Появится предупреждение:
```
⚠️ ВНИМАНИЕ!

Это запустит повторный поиск email для ВСЕХ компаний 
без email используя DeepSeek.

DeepSeek медленнее, но может найти email там, 
где Perplexity не нашел.

Процесс может занять несколько минут.

Продолжить?
```

### Шаг 4: Дождаться результатов
Система обработает все компании и покажет статистику.

## Преимущества DeepSeek

✅ **Более агрессивный промпт** - четкие инструкции что искать
✅ **JSON формат ответа** - структурированные данные
✅ **Confidence level** - оценка уверенности в результате
✅ **Дешевле Perplexity** - $0.14/1M input, $0.28/1M output
✅ **Умнее** - может найти email в сложных случаях

## Отличия от Stage 3

| Параметр | Stage 3 (Perplexity) | Stage 3 Retry (DeepSeek) |
|----------|---------------------|--------------------------|
| Модель | Perplexity Sonar Pro | DeepSeek Chat |
| Скорость | Быстро (параллельно) | Медленно (последовательно) |
| Цена | Дороже | Дешевле |
| Промпт | Стандартный | Агрессивный |
| Формат | Свободный текст | JSON обязательно |
| Валидация | Базовая | Строгая |

## SQL для проверки

```sql
-- Проверить сколько компаний готовы для retry
SELECT COUNT(*) as ready_for_retry
FROM pending_companies
WHERE stage3_status = 'failed'
  AND website IS NOT NULL
  AND (email IS NULL OR email = '')
  AND current_stage >= 2;

-- Посмотреть примеры
SELECT 
  company_name,
  website,
  stage3_status,
  current_stage,
  stage3_raw_data->>'source' as last_source
FROM pending_companies
WHERE stage3_status = 'failed'
  AND website IS NOT NULL
LIMIT 5;

-- Статистика после retry
SELECT 
  stage3_status,
  COUNT(*) as count,
  COUNT(CASE WHEN stage3_raw_data->>'source' = 'deepseek_retry' THEN 1 END) as via_deepseek
FROM pending_companies
WHERE website IS NOT NULL
GROUP BY stage3_status;
```

## Примеры промптов

### DeepSeek Промпт (Stage 3 Retry):
```
Тебе нужно найти корпоративный EMAIL-АДРЕС для китайской компании.

КОМПАНИЯ: 深圳韦克快速成型有限公司
ВЕБ-САЙТ: https://waykenrm.cn
ОПИСАНИЕ: CNC обработка, 3D печать
ТЕМА: Услуги обработки металла

ТВОЯ ЗАДАЧА:
1. Найди в интернете ОФИЦИАЛЬНЫЙ EMAIL этой компании
2. Проверь страницы: Contact Us, 联系我们, About, Footer
3. Ищи в: отраслевых каталогах, новостях, упоминаниях
4. Приоритет: info@, sales@, contact@, service@

КРИТИЧЕСКИ ВАЖНО:
❌ НЕ возвращай телефоны!
❌ НЕ ищи на маркетплейсах!
✅ Только корпоративный email

ФОРМАТ ОТВЕТА (ТОЛЬКО JSON):
{
  "email": "найденный@email.com или null",
  "source": "где нашел",
  "confidence": "high/medium/low"
}
```

### Perplexity Промпт (Stage 3):
```
Найди EMAIL-АДРЕС для этой компании через поиск в интернете:

КОМПАНИЯ: 深圳韦克快速成型有限公司
ГЛАВНАЯ СТРАНИЦА САЙТА: https://waykenrm.cn

Верни в JSON формате: {"emails": [...]}
```

## Тестирование

### 1. Подготовка
```bash
# Убедиться что есть компании для retry
node test-global-processing.js
```

### 2. Запуск
```bash
# Запустить сервер
npm start

# Открыть браузер
http://localhost:3030

# Нажать кнопку "🔄 Повторный поиск email (DeepSeek)"
```

### 3. Проверка логов
```bash
# Смотреть логи
tail -f logs/app.log | grep "Stage 3 Retry"
```

### 4. Проверка результатов
```sql
-- Посмотреть найденные email через DeepSeek
SELECT 
  company_name,
  email,
  stage3_raw_data->>'source' as source,
  stage3_raw_data->>'confidence' as confidence,
  stage3_status
FROM pending_companies
WHERE stage3_raw_data->>'source' = 'deepseek_retry';
```

## Ожидаемые результаты

При наличии 10 компаний без email:
- ✅ Обработано: 10
- ✅ Email найдено: 3-7 (30-70%)
- ⏱️ Время: ~20-40 секунд

DeepSeek может найти email в ~50% случаев, где Perplexity не нашел.

## Стоимость

**Пример для 100 компаний:**
- Input: ~50,000 токенов (500 токенов × 100)
- Output: ~10,000 токенов (100 токенов × 100)
- **Стоимость: ~$0.01** (очень дешево!)

**Сравнение:**
- Stage 3 Perplexity: ~$0.50 за 100 компаний
- Stage 3 Retry DeepSeek: ~$0.01 за 100 компаний

## Ограничения

⚠️ **DeepSeek НЕ имеет реального веб-поиска** (пока)
- Использует свои знания и данные из обучения
- Может "выдумать" email если не найдет реальный
- Поэтому важна валидация и проверка

✅ **Но все равно полезен:**
- Может найти email в своей базе знаний
- Лучше понимает контекст китайских сайтов
- Дешевле экспериментировать

## Будущие улучшения

1. **Интеграция реального веб-поиска** (когда DeepSeek добавит)
2. **Использование DeepSeek Reasoner** для сложных случаев
3. **Добавить confidence threshold** - сохранять только high confidence
4. **Email verification API** - проверять что email реально существует
5. **Retry несколько раз** - если confidence низкий

## Итог

✅ Функционал добавлен и готов к использованию
✅ Кнопка на главной странице
✅ API endpoint работает
✅ Документация готова
✅ Готово к тестированию

🎯 **Следующий шаг:** Протестировать на реальных данных!

