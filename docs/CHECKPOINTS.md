# 🔒 Система Checkpoints

Полная система создания и восстановления контрольных точек проекта, включая код и базу данных.

## 🎯 Что это?

**Checkpoint** - это полный снимок состояния системы на определённый момент времени:

- ✅ **Git коммит** - точная версия кода
- ✅ **База данных** - все таблицы (pending_companies, search_sessions, и т.д.)
- ✅ **Метаданные** - статистика, дата создания, размер
- ✅ **Контекст разговора** - описание состояния проекта (опционально)

## 📦 Быстрый старт

### Просмотр checkpoints

```bash
node scripts/checkpoint.js list
```

### Создание checkpoint

```bash
node scripts/checkpoint.js create v3.0.0
```

### Сохранение контекста разговора

```bash
# Интерактивный ввод
node scripts/checkpoint.js save-context v3.0.0

# Или одной строкой
node scripts/checkpoint.js save-context v3.0.0 "Стабильная версия после релиза"
```

### Восстановление checkpoint

```bash
node scripts/checkpoint.js restore v3.0.0
```

При восстановлении автоматически покажется сохранённый контекст!

### Информация о checkpoint

```bash
node scripts/checkpoint.js info v3.0.0
```

## 🔄 Сценарии использования

### 1. Перед серьёзными изменениями

```bash
# Создаём checkpoint текущего состояния
node scripts/checkpoint.js create v3.0.0

# Вносим изменения в код
# ...

# Если что-то пошло не так - возвращаемся
node scripts/checkpoint.js restore v3.0.0
```

### 2. Переход между версиями

```bash
# Смотрим доступные checkpoints
node scripts/checkpoint.js list

# Восстанавливаем нужную версию
node scripts/checkpoint.js restore v2.40.1
```

### 3. Тестирование новых функций

```bash
# Checkpoint перед тестированием
node scripts/checkpoint.js create v3.0.0-stable

# Разработка v4.0.0
# ...

# Если не получилось - откат
node scripts/checkpoint.js restore v3.0.0-stable
```

## ⚠️ Важно знать

### Что восстанавливается:

1. **Git код**
   - Переключает на нужный коммит
   - Создаёт новую ветку `restored-v3.0.0-<timestamp>`
   - Сохраняет текущие изменения в stash

2. **База данных**
   - ❌ УДАЛЯЕТ все текущие данные
   - ✅ Восстанавливает данные из checkpoint
   - Работает со всеми таблицами

3. **Зависимости**
   - Запускает `npm install`
   - Обновляет пакеты согласно checkpoint

### Подтверждение

При восстановлении система попросит подтверждение:

```
⚠️  ВНИМАНИЕ! Это действие:
   1. Переключит Git на коммит fab9a96
   2. Удалит ВСЕ текущие данные в базе данных
   3. Восстановит данные из checkpoint
   4. Обновит зависимости (npm install)

Продолжить? (yes/no):
```

Для автоматического подтверждения используйте флаг `--yes`:

```bash
node scripts/restore-checkpoint.js v3.0.0 --yes
```

## 📊 Структура checkpoint

```
checkpoints/
└── v3.0.0/
    ├── checkpoint.json    # Полный снимок (код + БД)
    ├── README.md          # Описание checkpoint
    └── info.txt           # Краткая информация
```

### checkpoint.json

```json
{
  "version": "v3.0.0",
  "created_at": "2024-12-01T12:00:00.000Z",
  "git_commit": "fab9a96...",
  "git_branch": "main",
  "database": {
    "pending_companies": [...],
    "pending_companies_ru": [...],
    "search_sessions": [...],
    "system_settings": [...]
  },
  "metadata": {
    "total_companies": 663,
    "with_email": 645,
    "validated": 654,
    "average_score": 61.1
  }
}
```

## 🚀 Рекомендации

### Именование версий

- `v3.0.0` - стабильная версия (релиз)
- `v3.0.0-stable` - проверенная стабильная версия
- `v3.1.0-beta` - тестовая версия
- `v3.0.0-before-refactor` - перед рефакторингом

### Когда создавать checkpoint:

✅ Перед серьёзными изменениями в коде
✅ Перед обновлением зависимостей
✅ После успешного релиза (стабильная версия)
✅ Перед экспериментами с БД
✅ Перед деплоем критических изменений

### Когда НЕ нужен checkpoint:

❌ Для мелких правок (опечатки, стили)
❌ Для коммитов "в процессе разработки"
❌ Слишком часто (захламляет диск)

## 💾 Размер и производительность

Типичный checkpoint:

- **Размер**: 3-5 MB (663 компании)
- **Создание**: 5-10 секунд
- **Восстановление**: 15-30 секунд

Checkpoint растёт с увеличением базы данных:

- 500 компаний → ~2.5 MB
- 1000 компаний → ~5 MB
- 5000 компаний → ~25 MB

## 🔧 Скрипты

### checkpoint.js

Главный менеджер checkpoints.

```bash
node scripts/checkpoint.js <command> [args]
```

**Команды:**
- `list` - показать все checkpoints
- `create <version>` - создать новый
- `restore <version>` - восстановить
- `info <version>` - информация

### create-checkpoint.js

Создание checkpoint напрямую.

```bash
node scripts/create-checkpoint.js v3.0.0
```

### restore-checkpoint.js

Восстановление checkpoint напрямую.

```bash
node scripts/restore-checkpoint.js v3.0.0
node scripts/restore-checkpoint.js v3.0.0 --yes  # без подтверждения
```

## 🐛 Troubleshooting

### "Checkpoint не найден"

```bash
# Проверьте список доступных
node scripts/checkpoint.js list

# Проверьте путь
ls -la checkpoints/
```

### "Ошибка Git"

Возможные причины:

1. Коммит не существует
2. Нет прав на Git операции
3. Конфликты с текущими изменениями

Решение:

```bash
# Сохраните изменения
git stash

# Попробуйте снова
node scripts/checkpoint.js restore v3.0.0
```

### "Ошибка базы данных"

Возможные причины:

1. Нет подключения к Supabase
2. Недостаточно прав
3. Таблицы не существуют

Проверка:

```bash
# Проверьте .env
cat .env | grep SUPABASE

# Проверьте подключение
node scripts/test-connection.js
```

## 📝 Примеры

### Пример 1: Безопасная разработка v4.0.0

```bash
# 1. Создаём checkpoint стабильной версии
node scripts/checkpoint.js create v3.0.0-stable

# 2. Начинаем разработку v4.0.0
git checkout -b feature/v4.0.0
# ... делаем изменения ...

# 3. Что-то пошло не так - откатываемся
node scripts/checkpoint.js restore v3.0.0-stable

# 4. После отката - перезапускаем сервер
npm start
```

### Пример 2: Тестирование на production

```bash
# 1. Checkpoint перед деплоем
node scripts/checkpoint.js create v3.0.0-production

# 2. Деплой новой версии
git push origin main
railway up

# 3. Обнаружили баг - быстрый откат
node scripts/checkpoint.js restore v3.0.0-production
railway up
```

### Пример 3: Сравнение версий

```bash
# Информация о текущей версии
node scripts/checkpoint.js info v3.0.0

# Информация о предыдущей версии
node scripts/checkpoint.js info v2.40.1

# Список всех версий
node scripts/checkpoint.js list
```

## 🎯 Best Practices

1. **Создавайте checkpoint перед каждым релизом**
   ```bash
   node scripts/checkpoint.js create v3.0.0
   git tag v3.0.0
   git push --tags
   ```

2. **Используйте осмысленные имена**
   ```bash
   # ✅ Хорошо
   v3.0.0-stable
   v3.1.0-before-ai-refactor
   v3.0.0-production-backup
   
   # ❌ Плохо
   test1
   backup
   tmp
   ```

3. **Очищайте старые checkpoints**
   ```bash
   # Удаляйте checkpoint старше 30 дней
   find checkpoints/ -mtime +30 -type d -exec rm -rf {} \;
   ```

4. **Документируйте checkpoint**
   - Добавляйте комментарии в Git
   - Обновляйте CHANGELOG.md
   - Отмечайте особенности версии

## ⚡ Быстрые команды

```bash
# Просмотр
alias cp-list="node scripts/checkpoint.js list"

# Создание
alias cp-create="node scripts/checkpoint.js create"

# Восстановление
alias cp-restore="node scripts/checkpoint.js restore"

# Информация
alias cp-info="node scripts/checkpoint.js info"
```

Добавьте в `~/.bashrc` или `~/.zshrc` для постоянного использования.

---

**Вопросы?** Используйте `node scripts/checkpoint.js help`

