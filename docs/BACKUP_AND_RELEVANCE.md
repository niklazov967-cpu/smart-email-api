# Backup & Relevance Filter - Документация

## Новые функции (v2.40.1)

### 1. Фильтр по релевантности

**Расположение:** Страница результатов (`/results.html`)

**Описание:** Добавлен новый фильтр для сортировки компаний по уровню релевантности на основе `validation_score`.

**Категории релевантности:**
- **Высокая (≥80)** - компании с высоким score, прошедшие AI-валидацию
- **Средняя (50-79)** - компании со средним score
- **Низкая (<50)** - компании с низким score
- **Без оценки** - компании, которые еще не прошли валидацию

**Использование:**
1. Откройте страницу результатов
2. В панели фильтров выберите "Релевантность (Score)"
3. Выберите нужную категорию
4. Нажмите "Применить"

**Технические детали:**
- Фильтр работает в связке с остальными фильтами (статус, поиск)
- Может комбинироваться с другими фильтрами
- Сохраняется при пагинации

---

### 2. Резервное копирование базы данных

**Расположение:** 
- Страница управления backup: `/backup.html`
- API endpoints: `/api/backup/*`
- Кнопка на главной странице: "💾 Backup БД"
- Кнопка на странице результатов: "💾 Backup БД"

**Возможности:**

#### 2.1. Создание backup

**Что сохраняется:**
- `pending_companies` - все найденные компании
- `pending_companies_ru` - русские переводы
- `search_sessions` - сессии поиска
- `system_settings` - системные настройки

**Формат файла:**
```json
{
  "metadata": {
    "created_at": "2025-12-01T12:00:00.000Z",
    "version": "1.0",
    "tables": ["pending_companies", ...]
  },
  "data": {
    "pending_companies": {
      "rows": [...],
      "count": 664
    },
    ...
  }
}
```

**Использование:**
1. Откройте `/backup.html` или используйте кнопку на главной/результатах
2. Нажмите "💾 Создать Backup"
3. Файл автоматически скачается в формате: `backup_YYYY-MM-DDTHH-MM-SS.json`

**API:**
```bash
# Создание backup
POST /api/backup/create

# Ответ:
{
  "success": true,
  "filename": "backup_2025-12-01T12-00-00.json",
  "stats": {
    "companies": 664,
    "translations": 320,
    "sessions": 12,
    "settings": 15,
    "size": 2458320
  },
  "downloadUrl": "/api/backup/download?data=..."
}
```

#### 2.2. Восстановление из backup

**Важно! ⚠️**
- Существующие данные будут обновлены (UPSERT)
- Процесс необратим
- Рекомендуется создать текущий backup перед восстановлением
- Лучше тестировать на тестовом окружении

**Использование:**
1. Откройте `/backup.html`
2. Нажмите "📁 Выбрать файл" и загрузите backup JSON
3. Проверьте информацию о файле в логе
4. Нажмите "🔄 Восстановить данные"
5. Подтвердите действие

**API:**
```bash
# Восстановление backup
POST /api/backup/restore
Content-Type: application/json

# Body: полный JSON backup файла

# Ответ:
{
  "success": true,
  "results": {
    "pending_companies": {
      "success": true,
      "inserted": 664
    },
    "pending_companies_ru": {
      "success": true,
      "inserted": 320
    },
    ...
  },
  "message": "Restore completed"
}
```

---

## Лучшие практики

### Backup
1. **Регулярность:**
   - Создавайте backup перед каждым значительным изменением
   - Периодический backup (ежедневно/еженедельно)
   - Backup перед обновлением версии

2. **Хранение:**
   - Храните в облачном хранилище (Google Drive, Dropbox)
   - Дублируйте на внешний диск
   - Используйте понятные имена с датой

3. **Проверка:**
   - Периодически проверяйте целостность файлов
   - Тестируйте восстановление на тестовом окружении

### Фильтр релевантности
1. **Приоритизация:**
   - Начните работу с компаниями высокой релевантности (≥80)
   - Проверьте компании без оценки - возможно нужна валидация

2. **Комбинирование:**
   - Используйте вместе с фильтром "С email"
   - Комбинируйте с поиском по названию

---

## Технические детали

### Архитектура

**Frontend:**
- `public/results.html` - фильтр релевантности
- `public/backup.html` - страница управления backup
- `public/index.html` - ссылка на backup

**Backend:**
- `src/api/backup.js` - API endpoints для backup/restore
- Регистрация в `src/app-simple.js`

### Безопасность

**Backup файлы:**
- Содержат все данные в открытом виде
- НЕ храните в публично доступных местах
- НЕ коммитьте в git

**Восстановление:**
- Проверка структуры файла перед восстановлением
- Используется UPSERT для избежания дубликатов
- Логирование всех операций

---

## Примеры использования

### Сценарий 1: Еженедельный backup
```bash
1. Каждую пятницу открыть /backup.html
2. Создать backup
3. Сохранить файл в Google Drive/Backups/Weekly/
4. Назвать: backup_week_49_2025.json
```

### Сценарий 2: Перед обновлением
```bash
1. Создать backup текущего состояния
2. Выполнить обновление (git pull, npm install)
3. Проверить работу системы
4. При ошибках - восстановить из backup
```

### Сценарий 3: Фильтрация топ-компаний
```bash
1. Открыть /results.html
2. Фильтр "Статус": С email
3. Фильтр "Релевантность": Высокая (≥80)
4. Экспорт в CSV
5. Использовать для рассылки
```

---

## Changelog

### v2.40.1 (2025-12-01)
- ✅ Добавлен фильтр по релевантности (validation_score)
- ✅ Реализована функция backup БД
- ✅ Реализована функция restore БД
- ✅ Создана страница управления backup
- ✅ Добавлены кнопки на главную и страницу результатов

---

## FAQ

**Q: Как часто нужно делать backup?**
A: Рекомендуется делать backup перед каждым значительным изменением (масштабный импорт, обновление) и периодически (раз в неделю).

**Q: Можно ли восстановить только одну таблицу?**
A: Нет, текущая версия восстанавливает все таблицы из backup. Для частичного восстановления отредактируйте JSON файл вручную.

**Q: Что делать если backup файл слишком большой?**
A: Для больших баз (>10MB) рассмотрите использование прямого экспорта PostgreSQL через Supabase Dashboard.

**Q: Безопасно ли восстанавливать backup на production?**
A: Всегда тестируйте восстановление на тестовом окружении. Создайте текущий backup перед восстановлением.

**Q: Почему фильтр релевантности показывает 0 компаний?**
A: Возможно компании еще не прошли Stage 4 (AI-валидацию). Запустите Stage 4 для получения validation_score.

