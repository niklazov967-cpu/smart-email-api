# Smart Email API - Портал сбора и обработки контактов

Система для автоматического поиска и сбора контактных данных китайских производителей с использованием трех API для оптимизации затрат:
- **DeepSeek API** - для генерации контента без интернета (экономия ~40%)
- **Perplexity Sonar Basic** - для простого поиска (URL, email)
- **Perplexity Sonar Pro** - для сложного анализа (компании, услуги)

## 🏗️ Архитектура

Система построена на принципе **пошагового дробления запросов** с оптимальным распределением по API:

1. **Query Expansion** → DeepSeek (генерация под-запросов)
2. **Этап 1**: Поиск компаний → Sonar Pro (критичный этап)
3. **Этап 2**: Поиск сайтов → Sonar Basic (простой поиск)
4. **Этап 3**: Анализ контактов → Sonar Basic (извлечение email)
5. **Этап 4**: Описание услуг → Sonar Pro (глубокий анализ)
6. **Этап 4.5**: Валидация компаний → DeepSeek (текстовый анализ)
7. **Этап 5**: Генерация тегов → DeepSeek (20 тегов на русском)
8. **Этап 6**: Финализация (локально)

### 💰 Экономия токенов: ~45-48%!

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Настройка окружения

```bash
cp .env.example .env
# Отредактируйте .env и добавьте ваши API ключи:
# - DEEPSEEK_API_KEY (обязательно)
# - PERPLEXITY_API_KEY (обязательно)
```

### Инициализация базы данных

```bash
# Создайте базу данных PostgreSQL
createdb sonar_portal

# Запустите миграции
npm run migrate

# Загрузите начальные настройки
npm run seed
```

### Запуск сервера

```bash
# Production
npm start

# Development (с автоперезагрузкой)
npm run dev
```

Сервер запустится на `http://localhost:3030`

## 📋 API Endpoints

### Основные

- `GET /` - Статус API
- `GET /health` - Проверка здоровья системы

### Сессии поиска

- `POST /api/sessions` - Создать новую сессию поиска
- `GET /api/sessions` - Список всех сессий
- `GET /api/sessions/:id` - Детали сессии
- `GET /api/sessions/:id/progress` - Прогресс выполнения

### Компании

- `GET /api/companies` - Список найденных компаний
- `GET /api/companies/:id` - Детали компании
- `POST /api/companies/export` - Экспорт данных (CSV/JSON/XLSX)

### Backup

- `POST /api/backup/create` - Создать резервную копию БД
- `GET /api/backup/download` - Скачать backup файл
- `POST /api/backup/restore` - Восстановить БД из backup

### Настройки

- `GET /api/settings` - Все настройки системы
- `GET /api/settings/:category` - Настройки по категории
- `PUT /api/settings/:category/:key` - Обновить настройку
- `POST /api/settings/:category/:key/reset` - Восстановить по умолчанию
- `GET /api/settings/:category/:key/history` - История изменений

## 🛠️ Компоненты системы

### Backend Services

- **DeepSeekClient** - Клиент для DeepSeek API (генерация без интернета)
- **SonarApiClient** - Клиент для Perplexity API (Basic и Pro модели)
- **SettingsManager** - Управление настройками системы
- **QueryOrchestrator** - Оркестратор обработки запросов
- **QueryExpander** - Генерация под-запросов (DeepSeek)
- **CompanyValidator** - Валидация компаний (DeepSeek)
- **CreditsTracker** - Отслеживание расходов API
- **ProgressTracker** - Живой прогресс-бар
- **ResponseParser** - Парсер ответов от API
- **CheckpointManager** - Сохранение состояния
- **CacheManager** - Управление кешем (Redis)

### Database Tables

- `settings` - Хранилище настроек (117 параметров)
- `settings_history` - История изменений
- `search_sessions` - Сессии поиска
- `pending_companies` - Компании на обработке
- `processed_websites` - Проанализированные сайты
- `company_records` - Финальные записи компаний
- `perplexity_cache` - Кеш Sonar результатов
- `sonar_api_calls` - История вызовов API

## 📊 Параметры системы

Система поддерживает 117 настраиваемых параметров в 12 категориях:

1. **API Perplexity** (11 параметров)
2. **Этапы обработки** (14 параметров)
3. **База данных** (7 параметров)
4. **Кеш** (6 параметров)
5. **Очередь задач** (8 параметров)
6. **Валидация** (10 параметров)
7. **Логирование** (11 параметров)
8. **Сессия поиска** (7 параметров)
9. **Экспорт данных** (8 параметров)
10. **Уведомления** (10 параметров)
11. **Безопасность** (8 параметров)
12. **Продвинутые** (15 параметров)

Все параметры можно изменять через UI без перезапуска приложения.

## 🔧 Технологии

- **Backend**: Node.js + Express
- **Database**: PostgreSQL
- **Cache**: Redis
- **API**: Perplexity Sonar Pro
- **Logging**: Winston

## 📈 Примеры использования

### Создание сессии поиска

```bash
curl -X POST http://localhost:3030/api/sessions \
  -H "Content-Type: application/json" \
  -d '{
    "search_query": "不锈钢数控车铣加工",
    "target_count": 50,
    "priority": "quality"
  }'
```

### Экспорт данных

```bash
curl -X POST http://localhost:3030/api/companies/export \
  -H "Content-Type: application/json" \
  -d '{
    "format": "csv",
    "session_id": "uuid-here"
  }'
```

## 🔒 Безопасность

- API ключи маскируются в логах
- Поддержка IP whitelist
- JWT аутентификация
- Rate limiting
- HTTPS требуется в production

## ✨ Новые функции (v2.40.1)

### Фильтр по релевантности
- Фильтрация компаний по `validation_score`
- Категории: Высокая (≥80), Средняя (50-79), Низкая (<50), Без оценки
- Комбинируется с другими фильтрами
- Доступен на странице результатов (`/results.html`)

### Резервное копирование БД
- Создание backup всех таблиц в JSON формат
- Восстановление из backup файла
- Страница управления: `/backup.html`
- API endpoints: `/api/backup/*`
- Статистика и логирование операций

Подробнее: [docs/BACKUP_AND_RELEVANCE.md](docs/BACKUP_AND_RELEVANCE.md)

## 📝 Лицензия

ISC
