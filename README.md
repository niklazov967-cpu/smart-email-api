# Smart Email API

Простой и умный API для отправки email сообщений.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск сервера

```bash
npm start
```

Сервер запустится на `http://localhost:3030`

## 📋 API Endpoints

### GET /
Главная страница API с информацией о версии и статусе.

**Ответ:**
```json
{
  "message": "Smart Email API",
  "status": "running",
  "version": "1.0.0"
}
```

### GET /health
Проверка состояния сервера.

**Ответ:**
```json
{
  "status": "OK",
  "timestamp": "2025-11-26T21:00:00.000Z"
}
```

### POST /api/email/send
Отправка email сообщения.

**Параметры запроса:**
```json
{
  "to": "example@email.com",
  "subject": "Тема письма",
  "body": "Текст письма"
}
```

**Ответ:**
```json
{
  "message": "Email endpoint ready",
  "data": {
    "to": "example@email.com",
    "subject": "Тема письма",
    "body": "Текст письма"
  }
}
```

## 🔧 Конфигурация

Создайте файл `.env` в корне проекта:

```env
PORT=3030
```

## 📦 Технологии

- Node.js
- Express.js
- CORS
- dotenv

## 📝 Лицензия

ISC

