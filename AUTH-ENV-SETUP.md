# 🔐 Настройка авторизации для Smart Email API

## Добавьте в ваш `.env` файл:

```bash
# ==============================================
# АВТОРИЗАЦИЯ
# ==============================================

# Учетные данные для входа на сайт
AUTH_USERNAME=admin
AUTH_PASSWORD=admin123

# Секретный ключ для сессий (измените на случайную строку!)
SESSION_SECRET=smart-email-api-secret-key-change-in-production-to-random-string
```

## Важно!

1. **Измените пароль** - замените `admin123` на ваш безопасный пароль
2. **Измените секретный ключ** - замените SESSION_SECRET на случайную строку (минимум 32 символа)
3. **Не коммитьте .env** в git - файл уже в `.gitignore`

## Генерация безопасного секретного ключа:

```bash
# В терминале:
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

Скопируйте результат и используйте как `SESSION_SECRET`.

