# 🚀 Деплой с авторизацией на Railway.app

## ✅ Код отправлен на GitHub

Автоматический деплой на Railway начнется через несколько секунд.

---

## ⚠️ ВАЖНО: Добавьте переменные окружения!

### Откройте Railway Dashboard:

```
https://railway.app/project/d51a9b81-1256-4083-bc94-2d895e79db57
```

### Перейдите в Settings → Variables

### Добавьте 3 новые переменные:

```bash
AUTH_USERNAME=admin
AUTH_PASSWORD=ваш_безопасный_пароль
SESSION_SECRET=случайная_строка_32_символа
```

### Генерация безопасного SESSION_SECRET:

Выполните в терминале:

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

Скопируйте результат и используйте как `SESSION_SECRET`.

---

## 📋 Инструкция по добавлению переменных:

### Вариант 1: Через веб-интерфейс (рекомендуется)

1. Откройте https://railway.app/project/d51a9b81-1256-4083-bc94-2d895e79db57
2. Выберите ваш сервис
3. Settings → Variables
4. Нажмите "+ New Variable"
5. Добавьте каждую переменную:
   - `AUTH_USERNAME` = `admin`
   - `AUTH_PASSWORD` = `your_secure_password`
   - `SESSION_SECRET` = `generated_random_string`
6. Сохраните

### Вариант 2: Через Railway CLI (если настроен)

```bash
railway variables set AUTH_USERNAME=admin
railway variables set AUTH_PASSWORD=your_secure_password
railway variables set SESSION_SECRET=generated_random_string
```

---

## 🔍 Проверка деплоя

### 1. Дождитесь завершения деплоя

В Railway Dashboard → Deployments → статус должен быть "Success" (зеленый)

### 2. Откройте ваш сайт:

```
https://smart-email-api-production.up.railway.app
```

### 3. Проверьте авторизацию:

- Вас должно перенаправить на `/login.html`
- Введите логин и пароль
- После входа - доступ к сайту

---

## ✅ Что деплоится:

### Новые функции:

1. **Авторизация**
   - Страница логина
   - Защита всех API и страниц
   - Сессии на 24 часа

2. **Улучшенная дедупликация**
   - Нормализация URL (без www, путей)
   - Дедупликация поддоменов (us.example.com → example.com)
   - Проверка между сессиями в БД
   - Поддержка двойных TLD (.co.uk, .com.cn)

### Технические изменения:

- `express-session` для сессий
- Middleware авторизации
- API endpoints для логина/логаута
- Улучшенный метод `_extractMainDomain`

---

## 🧪 После деплоя проверьте:

```bash
# 1. Проверка доступности
curl https://smart-email-api-production.up.railway.app/

# 2. Проверка что требуется авторизация
curl https://smart-email-api-production.up.railway.app/api
# Должно вернуть: {"success":false,"error":"Unauthorized"}

# 3. Проверка страницы логина
curl -I https://smart-email-api-production.up.railway.app/login.html
# Должно вернуть: HTTP 200
```

---

## 📊 Чек-лист деплоя:

- ✅ Код отправлен на GitHub
- ⏳ Railway автоматически деплоит
- ⚠️ **Добавьте переменные окружения!**
- ⏳ Дождитесь завершения деплоя
- ⏳ Проверьте работу авторизации

---

## 🔐 Безопасность:

### ⚠️ ОБЯЗАТЕЛЬНО:

1. **Измените пароль** - не используйте `admin123` в production!
2. **Генерируйте SESSION_SECRET** - не используйте значение по умолчанию
3. **Используйте HTTPS** - Railway предоставляет автоматически

### Рекомендуемый пароль:

Минимум:
- 12 символов
- Буквы (A-Z, a-z)
- Цифры (0-9)
- Спецсимволы (!@#$%^&*)

Пример безопасного пароля:
```
MySecure#Pass2024!
```

---

## 📝 Примечания:

### Railway автоматически:

- ✅ Установит `express-session` из `package.json`
- ✅ Перезапустит сервер
- ✅ Применит новый код
- ✅ Настроит HTTPS

### Вам нужно вручную:

- ⚠️ Добавить переменные AUTH_*
- ⚠️ Перезапустить сервис (если уже запущен)

---

## 🎯 Итого:

**Время деплоя:** ~2-3 минуты

**Что делать сейчас:**
1. Подождите завершения автодеплоя
2. Добавьте переменные в Railway Dashboard
3. Откройте сайт и проверьте авторизацию

**URL:** https://smart-email-api-production.up.railway.app

---

**✅ Готово!** Ваш сайт теперь с авторизацией и улучшенной дедупликацией! 🚀🔒

