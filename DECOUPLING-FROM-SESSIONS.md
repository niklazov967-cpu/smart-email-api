# Отвязка обработки от сессий

## Дата: 29 ноября 2025

## Проблема

При нажатии кнопки "Очистить прогресс" сбрасывался только `processing_progress`, но не статусы компаний (`current_stage`, `stageX_status`). 

Также обработка этапов 2-4 была жестко привязана к конкретной сессии, что не позволяло:
- Обрабатывать все компании в базе независимо от сессии
- Продолжать обработку компаний из разных сессий одновременно
- Эффективно использовать ресурсы API

## Решение

### 1. Исправлена кнопка "Очистить прогресс"

**Файл:** `src/api/sessions.js`

Теперь `DELETE /api/sessions/:id/clear-progress` также сбрасывает:
- `current_stage` → 1
- `stage1_status` → 'completed' (компании уже найдены)
- `stage2_status`, `stage3_status`, `stage4_status` → NULL
- `validation_score`, `validation_reason`, `ai_generated_description`, `ai_confidence_score` → NULL

**НЕ очищает:**
- Найденные данные: `website`, `email`, `description`, `tags`
- Сами компании из `pending_companies`

### 2. Добавлено поле `topic_description` в `pending_companies`

**Файл:** `database/add-topic-to-companies.sql`

```sql
ALTER TABLE pending_companies 
ADD COLUMN IF NOT EXISTS topic_description TEXT;

UPDATE pending_companies pc
SET topic_description = ss.topic_description
FROM search_sessions ss
WHERE pc.session_id = ss.session_id
  AND pc.topic_description IS NULL;
```

Теперь каждая компания имеет:
- `session_id` - откуда она была найдена
- `search_query_text` - конкретный подзапрос
- `topic_description` - **НОВОЕ**: главная тема поиска

### 3. Stage 1: Сохранение темы при создании компании

**Файл:** `src/stages/Stage1FindCompanies.js`

- Загружает `topic_description` из `search_sessions` при старте
- Присваивает каждой компании `company.topicDescription`
- Сохраняет в поле `topic_description` при INSERT

### 4. Stage 2-4: Убрана привязка к `session_id`

**Измененные файлы:**
- `src/stages/Stage2FindWebsites.js`
- `src/stages/Stage3AnalyzeContacts.js`
- `src/stages/Stage4AnalyzeServices.js`

**Изменения:**

#### `execute(sessionId = null)`
- `sessionId` теперь **опциональный** параметр
- Если `sessionId` не указан → обрабатываются **ВСЕ** компании в базе
- Если `sessionId` указан → работает как раньше (только для этой сессии)

#### `_getCompanies(sessionId = null)`
- Выбирает компании по `current_stage` и `stageX_status`
- Если `sessionId` не указан → выбирает из всей базы
- Если `sessionId` указан → фильтрует по `.eq('session_id', sessionId)`

#### Stage 4: Использование `topic_description` из компании
- **Раньше:** Загружал `topic_description` из `search_sessions` для всей сессии
- **Теперь:** Использует `company.topic_description` напрямую из БД
- Каждая компания обрабатывается со своей темой, даже если они из разных сессий

## Преимущества новой архитектуры

### ✅ Гибкость
- Можно обрабатывать все компании в базе одной командой
- Можно обрабатывать только конкретную сессию (для обратной совместимости)
- Можно добавлять компании из разных сессий и обрабатывать их вместе

### ✅ Эффективность
- Нет необходимости запускать обработку для каждой сессии отдельно
- Можно обрабатывать 1000+ компаний из разных сессий за один запуск
- Меньше простоя API между сессиями

### ✅ Масштабируемость
- База данных может содержать компании из сотен сессий
- Обработка не зависит от количества сессий
- Легко добавлять новые компании без нарушения существующей обработки

### ✅ Прозрачность
- Каждая компания имеет свою тему и запрос
- Stage 4 корректно валидирует компанию по её собственной теме
- Логи показывают `sessionId: 'ALL'` или конкретный ID

## Как использовать

### Глобальная обработка (все компании)

```javascript
// Stage 2: Найти сайты для ВСЕХ компаний без сайта
await stage2.execute();

// Stage 3: Найти email для ВСЕХ компаний с сайтом, но без email
await stage3.execute();

// Stage 4: Валидировать ВСЕ компании прошедшие Stage 3
await stage4.execute();
```

### Обработка конкретной сессии (как раньше)

```javascript
const sessionId = 'abc-123';

await stage2.execute(sessionId);
await stage3.execute(sessionId);
await stage4.execute(sessionId);
```

## Обратная совместимость

✅ Все существующие API endpoints работают как раньше
✅ UI для пошаговой обработки не изменен
✅ Логика определения `current_stage` не изменена
✅ Stage 1 всё ещё привязан к сессии (т.к. создаёт компании)

## Следующие шаги

### 1. Обновить UI
- Добавить раздел "🌍 Глобальная обработка"
- Показывать статистику по всей базе:
  - Всего компаний в БД
  - Готовы для Stage 2 (без сайта)
  - Готовы для Stage 3 (с сайтом, без email)
  - Готовы для Stage 4 (с email, не валидированы)
- Кнопки для глобальной обработки каждого этапа

### 2. API endpoints для глобальной обработки

```javascript
// Новые endpoints:
POST /api/processing/stage2/all  // Запустить Stage 2 для всех
POST /api/processing/stage3/all  // Запустить Stage 3 для всех
POST /api/processing/stage4/all  // Запустить Stage 4 для всех
GET  /api/processing/stats       // Статистика по всей базе
```

### 3. Тестирование

- Создать 2-3 разные сессии
- Запустить Stage 1 для каждой (компании с разными темами)
- Запустить `stage2.execute()` без sessionId
- Проверить что обработаны компании из ВСЕХ сессий
- Проверить что каждая компания валидируется по своей теме в Stage 4

## SQL для применения

```bash
# 1. Добавить поле topic_description
psql -h <host> -U <user> -d <db> -f database/add-topic-to-companies.sql

# 2. Или выполнить в Supabase SQL Editor:
```

```sql
-- Добавить поле
ALTER TABLE pending_companies 
ADD COLUMN IF NOT EXISTS topic_description TEXT;

-- Заполнить из search_sessions
UPDATE pending_companies pc
SET topic_description = ss.topic_description
FROM search_sessions ss
WHERE pc.session_id = ss.session_id
  AND pc.topic_description IS NULL;

-- Индекс
CREATE INDEX IF NOT EXISTS idx_pending_companies_topic 
ON pending_companies (topic_description);
```

## Проверка

```sql
-- Проверить что поле добавлено
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'pending_companies' 
  AND column_name = 'topic_description';

-- Проверить что данные заполнены
SELECT 
  COUNT(*) as total,
  COUNT(topic_description) as with_topic,
  COUNT(*) - COUNT(topic_description) as without_topic
FROM pending_companies;

-- Посмотреть примеры
SELECT 
  company_name, 
  search_query_text, 
  topic_description,
  current_stage
FROM pending_companies
LIMIT 5;
```

## Итог

✅ Кнопка "Очистить прогресс" теперь полностью сбрасывает статусы компаний
✅ Обработка Stages 2-4 отвязана от сессий
✅ Каждая компания имеет свою тему (`topic_description`)
✅ Stage 4 валидирует каждую компанию по её собственной теме
✅ Обратная совместимость сохранена
✅ Готово к глобальной обработке тысяч компаний

🎯 **Следующий шаг:** Обновить UI для глобальной обработки или начать тестирование

