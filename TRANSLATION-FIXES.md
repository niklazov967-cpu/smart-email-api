# Исправления системы переводов

## 🔧 Выполненные исправления

### 1. ✅ Теги теперь показывают русский перевод

**Проблема:** Теги отображались только на китайском/английском, перевод был только во всплывающей подсказке.

**Решение:** 
- Если есть перевод тега - показываем русский текст
- Оригинал (китайский) показываем во всплывающей подсказке при наведении
- Если перевода нет - показываем оригинал

**Файл:** `public/results.html` - строки 1164-1176

**Пример:**
- Было: `CNC machining` (во всплывающей подсказке: "🇷🇺 ЧПУ обработка")
- Стало: `ЧПУ обработка` (во всплывающей подсказке: "🇨🇳 CNC machining")

---

### 2. ✅ Английские тексты теперь переводятся

**Проблема:** Длинные английские описания и сервисы не переводились, т.к. система считала их "терминами".

**Решение:**
- Короткие тексты (≤50 символов) на английском = технические термины → НЕ переводим (CNC, CAD, ISO)
- Длинные тексты (>50 символов) на английском = полные предложения → ПЕРЕВОДИМ

**Файл:** `src/services/TranslationService.js` - метод `_isLatin()` строки 428-448

**Логика:**
```javascript
if (totalChars > 50 && 80% латиница) {
  return false; // НЕ пропускаем - переводим длинные тексты
}
return true; // Пропускаем короткие термины
```

---

### 3. ✅ Автоматический retry для ошибок перевода

**Проблема:** Если перевод падал с ошибкой, он больше не пытался перевести это поле.

**Решение:**
- Добавлен счетчик `retry_count` в таблицу `translations`
- При ошибке перевода: retry_count увеличивается
- Максимум 3 попытки (`this.maxRetries = 3`)
- После 3 неудачных попыток - поле помечается как "failed окончательно"
- При успешном переводе - retry_count сбрасывается в 0

**Файлы:** 
- `src/services/TranslationService.js` - методы `translateCompany()` и `saveTranslation()`
- Строки 134-171, 251-287

**Логика retry:**
```
Попытка 1: failed → retry_count = 1
Попытка 2: failed → retry_count = 2  
Попытка 3: failed → retry_count = 3 → больше не пытаемся
Попытка 1: success → retry_count = 0 → перевод сохранен
```

---

### 4. ✅ Улучшено отображение ошибок в UI

**Проблема:** Непонятно почему у компании показывается "ошибка перевода".

**Решение:**
- ✓ RU - успешно переведено (зелёный badge)
- ⏳ - перевод в процессе (жёлтый badge)
- ⚠️ - ошибка перевода, будет повторная попытка (красный badge с подсказкой)

**Файл:** `public/results.html` - строки 941-952

**Подсказка при наведении:**
- Completed: "Переведено"
- Pending: "Перевод в процессе"
- Failed: "Ошибка перевода - будет повторная попытка"

---

## 📊 Что изменилось в работе системы

### Теперь переводятся:
1. ✅ Теги (tag1-tag20) - показываются на русском
2. ✅ Длинные английские описания
3. ✅ Английские сервисы
4. ✅ Validation_reason на английском

### НЕ переводятся (правильно):
1. ✅ Короткие технические термины (CNC, CAD, ISO, 3D, 5-axis)
2. ✅ Аббревиатуры
3. ✅ Уже переведенные поля

### Retry логика:
- 🔄 Автоматически повторяет failed переводы (до 3 раз)
- ⏸️ Останавливается после 3 неудачных попыток
- 📊 Логирует каждую попытку с номером

---

## 🧪 Тестирование

### Как проверить исправления:

1. **Перезапустить сервер:**
```bash
cd "/path/to/smart-email-api"
lsof -ti:3030 | xargs kill -9 2>/dev/null
sleep 3
node src/app-simple.js 2>&1 &
```

2. **Открыть results.html:**
```
http://localhost:3030/results.html
```

3. **Проверить теги:**
- Открыть карточку компании
- Раздел "Все теги"
- Должны показываться русские переводы
- При наведении - китайский/английский оригинал

4. **Проверить английские тексты:**
- Найти компанию с английским описанием (>50 символов)
- Открыть карточку
- Должен быть русский перевод под 🇷🇺

5. **Проверить retry:**
- Посмотреть компании с ⚠️ (ошибка перевода)
- Подождать работы translation worker
- Должна быть повторная попытка перевода

---

## 🚀 Перезапуск translation worker

Если worker уже запущен, его нужно перезапустить для применения изменений:

```bash
# Остановить старый worker
pkill -f translationWorker

# Запустить новый
npm run translate:start

# Или вручную:
node src/workers/translationWorker.js
```

---

## 📝 Конфигурация

### Настройки в `TranslationService.js`:

```javascript
this.maxRetries = 3;                    // Максимум попыток перевода
this.delayBetweenRequests = 1500;      // Задержка между запросами (мс)
```

### Приоритет полей для перевода:

1. **Priority 1:** company_name (название компании)
2. **Priority 2:** description, ai_generated_description
3. **Priority 3:** services, validation_reason
4. **Priority 4:** tag1-tag20

---

## 🐛 Известные ограничения

1. **Длинные термины:**
   - Если технический термин длиннее 50 символов, он будет переведен
   - Решение: можно добавить словарь исключений

2. **Смешанный текст:**
   - Текст "CNC обработка металла" может быть пропущен
   - Решение: улучшить алгоритм определения языка

3. **Retry после изменений:**
   - Уже failed переводы (retry_count >= 3) не будут повторяться автоматически
   - Решение: можно вручную удалить запись из translations таблицы

---

## 📦 Измененные файлы

1. `public/results.html` - отображение тегов и ошибок
2. `src/services/TranslationService.js` - логика перевода и retry
3. `TRANSLATION-FIXES.md` - эта документация

---

**Дата:** 28 ноября 2025  
**Версия:** 1.3.1  
**Статус:** ✅ Готово к тестированию

