#!/usr/bin/env node

const dm = require('../src/utils/DomainPriorityManager.js');

console.log(`
╔════════════════════════════════════════════════════════════════╗
║           ТЕСТ: ПРИОРИТЕТЫ ДОМЕНОВ ДЛЯ КИТАЙСКИХ ПОСТАВЩИКОВ  ║
╚════════════════════════════════════════════════════════════════╝
`);

// Тест 1: wayken
console.log(`
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Тест 1: Wayken - три домена
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
`);
dm.logComparison('wayken.cn', 'wayken.com');
dm.logComparison('wayken.cn', 'wayken.net');

const waykenDomains = ['wayken.com', 'wayken.net', 'wayken.cn'];
const bestWayken = dm.selectBest(waykenDomains);
console.log('🏆 Лучший домен для Wayken:', bestWayken);

// Тест 2: gensun
console.log(`
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Тест 2: Gensun - .com vs .com.cn
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
`);
dm.logComparison('gensun.com', 'gensun.com.cn');

const gensunDomains = ['gensun.com', 'gensun.com.cn', 'https://www.gensun.com'];
const bestGensun = dm.selectBest(gensunDomains);
console.log('🏆 Лучший домен для Gensun:', bestGensun);

// Тест 3: Проверка isSameCompany
console.log(`
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Тест 3: Проверка одной компании
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
`);
console.log('wayken.cn vs wayken.com:', dm.isSameCompany('wayken.cn', 'wayken.com') ? '✅ Одна компания' : '❌ Разные');
console.log('wayken.cn vs runsom.cn:', dm.isSameCompany('wayken.cn', 'runsom.cn') ? '✅ Одна компания' : '❌ Разные');

// Тест 4: Все TLD
console.log(`
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Тест 4: Приоритеты всех TLD
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
`);
const domains = [
  'example.com',
  'example.cn',
  'example.com.cn',
  'example.net',
  'example.net.cn',
  'example.io',
  'example.asia'
];

domains.forEach(d => {
  const info = dm.getDomainInfo(d);
  console.log(`${info.isChinese ? '🇨🇳' : '🌍'} ${d.padEnd(20)} → Priority: ${info.priority.toString().padStart(3)} | TLD: ${info.tld}`);
});

console.log(`
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ ВСЕ ТЕСТЫ ЗАВЕРШЕНЫ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
`);

