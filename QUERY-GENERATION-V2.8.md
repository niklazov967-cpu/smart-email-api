# Обновление генерации запросов v2.8.1

## 📋 Описание изменений

### ✅ Автоматическое сохранение (v2.8.0)
- Запросы с релевантностью **≥70%** автоматически добавляются в базу
- Удалено ручное подтверждение и чекбоксы
- Автоматический выбор созданной темы в Stage 1
- Плавная прокрутка к Stage 1 после генерации

### 📊 Прогресс генерации (v2.8.0)
```
⚡ Генерация запросов                     45%
Проход: 3 из 5
⏳ Генерация 50 запросов...
```

- Визуальный прогресс-бар
- Отображение текущего прохода
- Статус: генерация / сохранение / завершено

### 🎯 Статистика (v2.8.1)
```
📊 Статистика:
Всего сгенерировано: 150 запросов
Релевантные (≥70%): 120 запросов
Добавлено в базу: 115 запросов
Дубликатов отфильтровано: 5 запросов

🎯 Уникальных подзапросов добавлено: 115
```

### ⚙️ Параметры
- **Количество под-запросов за проход:** 10-50
- **Количество проходов генерации:** 1-100 *(увеличено с 10)*

## 🎨 Интерфейс

### Поля ввода
```
┌─────────────────────────────────────────────┐
│ Введите основную тему:                      │
│ ┌─────────────────────────────────────────┐ │
│ │ Производители деталей для обработки...  │ │
│ └─────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘

┌──────────────────────┬──────────────────────┐
│ Количество под-запр. │ Количество проходов  │
│ ┌──────────────────┐ │ ┌──────────────────┐ │
│ │ 50 (10-50)       │ │ │ 1 (1-100)        │ │
│ └──────────────────┘ │ └──────────────────┘ │
└──────────────────────┴──────────────────────┘
```

### Отображение результатов
```
✅ Релевантные запросы добавлены в базу

📊 Статистика:
  Всего сгенерировано: 150 запросов
  Релевантные (≥70%): 120 запросов
  Добавлено в базу: 115 запросов
  Дубликатов отфильтровано: 5 запросов
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  🎯 Уникальных подзапросов добавлено: 115

📝 Добавленные запросы с рейтингом:

┌───────────────────────────────────────────┬─────┐
│ 数控加工小批量生产                         │ 85  │
│ Производство малых партий на ЧПУ          │Высок│
└───────────────────────────────────────────┴─────┘

┌───────────────────────────────────────────┬─────┐
│ 精密零件CNC加工厂家                        │ 78  │
│ Производители CNC обработки деталей       │Сред.│
└───────────────────────────────────────────┴─────┘
```

## 🔄 Рабочий процесс

### 1. Генерация
```
Проход 1: 50 запросов → фильтр ≥70% → 42 релевантных
Проход 2: 50 запросов → фильтр ≥70% → 38 релевантных
Проход 3: 50 запросов → фильтр ≥70% → 40 релевантных
```

### 2. Дедупликация
```
Всего релевантных: 120 запросов
- Дубликаты по query_cn: 5 запросов
= Уникальных: 115 запросов
```

### 3. Сохранение
```
115 уникальных запросов → база данных
Session ID создан: 5eea1bfc-4693-411e-915d-d75a25fda2d8
Тема автоматически выбрана в Stage 1
```

## 💡 Преимущества

### Скорость
- ✅ Нет ручного подтверждения
- ✅ Автоматическая фильтрация по релевантности
- ✅ Пакетная обработка до 100 проходов

### Прозрачность
- ✅ Реальный прогресс генерации
- ✅ Детальная статистика
- ✅ Подсчет уникальных запросов
- ✅ Отображение рейтингов

### UX
- ✅ Один клик для генерации
- ✅ Автоматический переход к Stage 1
- ✅ Визуальная обратная связь
- ✅ Блокировка кнопки во время процесса

## 📈 Пример использования

### Сценарий: 5 проходов по 50 запросов

**Входные данные:**
```
Тема: "Производители точных деталей на ЧПУ"
Запросов за проход: 50
Количество проходов: 5
```

**Процесс:**
```
Проход 1/5 (0%)   → Генерация → 50 запросов
                  → Фильтр ≥70% → 42 релевантных
                  → Сохранение → 42 добавлено

Проход 2/5 (20%)  → Генерация → 50 запросов
                  → Фильтр ≥70% → 38 релевантных
                  → Сохранение → 35 добавлено (3 дубликата)

Проход 3/5 (40%)  → Генерация → 50 запросов
                  → Фильтр ≥70% → 40 релевантных
                  → Сохранение → 38 добавлено (2 дубликата)

Проход 4/5 (60%)  → Генерация → 50 запросов
                  → Фильтр ≥70% → 35 релевантных
                  → Сохранение → 30 добавлено (5 дубликатов)

Проход 5/5 (80%)  → Генерация → 50 запросов
                  → Фильтр ≥70% → 37 релевантных
                  → Сохранение → 32 добавлено (5 дубликатов)

Завершено (100%)
```

**Результат:**
```
📊 Статистика:
  Всего сгенерировано: 250 запросов
  Релевантные (≥70%): 192 запросов
  Добавлено в базу: 177 запросов
  Дубликатов отфильтровано: 15 запросов
  
  🎯 Уникальных подзапросов добавлено: 177
```

## 🔧 Техническая реализация

### Алгоритм уникальности
```javascript
const uniqueQueries = new Set();
queries.forEach(q => {
  const queryCn = q.query_cn || q.chinese_query || q.query;
  uniqueQueries.add(queryCn.toLowerCase().trim());
});
const uniqueSavedCount = uniqueQueries.size;
```

### Фильтр релевантности
```javascript
const relevantQueries = queries.filter(q => {
  const relevance = q.relevance || q.relevance_score || 0;
  return relevance >= 70;
});
```

### Прогресс
```javascript
for (let pass = 1; pass <= numPasses; pass++) {
  const progressPercent = Math.round(((pass - 1) / numPasses) * 100);
  updateProgress(progressPercent, pass, numPasses);
  
  // Generate & Save
  await generateAndSave();
  
  // Delay between passes
  if (pass < numPasses) await sleep(500);
}
```

## 📝 Версии

- **v2.8.0** - Автоматическое сохранение с прогрессом
- **v2.8.1** - Отображение уникальных подзапросов + лимит 100 проходов

## 🎯 Следующие шаги

Возможные улучшения:
- [ ] Экспорт списка запросов в CSV
- [ ] История генераций
- [ ] Фильтры по релевантности (настраиваемый порог)
- [ ] Предпросмотр дубликатов перед сохранением
- [ ] Пауза/возобновление генерации

---

**Дата:** 30.11.2025  
**Версия:** 2.8.1  
**Коммит:** 9d95e2e

