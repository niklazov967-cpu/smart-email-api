# 🌳 Git Branches Strategy

## Структура веток

```
main (v2.1.0 - stable)
│
├── release/v2.1 ← 📦 Стабильная версия 2.1 (для hotfixes)
│   └── v2.1.0 (tag)
│
└── v3.0-development ← 🚀 Разработка версии 3.0 (ТЕКУЩАЯ)
```

---

## 📋 Описание веток

### `main`
- **Статус:** Production-ready
- **Версия:** v2.1.0
- **Назначение:** Основная стабильная ветка
- **Deployment:** Автоматически деплоится на Railway

### `release/v2.1`
- **Статус:** Maintenance
- **Версия:** v2.1.x
- **Назначение:** Поддержка версии 2.1 (критические фиксы)
- **Когда использовать:** Если нужно исправить баг в v2.1 не затрагивая v3.0

### `v3.0-development` ⭐ **ВЫ ЗДЕСЬ**
- **Статус:** Active Development
- **Версия:** v3.0.0-alpha
- **Назначение:** Разработка новых фич для версии 3.0
- **Свобода:** Можно экспериментировать, ломать, переписывать

---

## 🚀 Команды для работы

### Начать работу над v3.0 (уже сделано)
```bash
git checkout v3.0-development
```

### Коммитить изменения в v3.0
```bash
git add .
git commit -m "feat: новая фича для v3.0"
git push origin v3.0-development
```

### Вернуться к стабильной v2.1
```bash
# Смотреть код v2.1
git checkout release/v2.1

# Исправить баг в v2.1
git checkout release/v2.1
# ... сделать изменения ...
git add .
git commit -m "fix: исправление бага в v2.1"
git push origin release/v2.1

# Можно также смержить фикс в main
git checkout main
git merge release/v2.1
```

### Вернуться к разработке v3.0
```bash
git checkout v3.0-development
```

### Посмотреть список веток
```bash
git branch -a
```

### Посмотреть текущую ветку
```bash
git branch --show-current
```

---

## 🎯 Когда v3.0 готова к релизу

### Шаг 1: Обновить версию
```bash
# В package.json изменить version на "3.0.0"
```

### Шаг 2: Смержить в main
```bash
git checkout main
git merge v3.0-development
```

### Шаг 3: Создать релизный тег
```bash
git tag -a v3.0.0 -m "Release v3.0.0"
git push origin main
git push origin v3.0.0
```

### Шаг 4: Опционально - создать release ветку для v3.0
```bash
git checkout -b release/v3.0
git push origin release/v3.0
```

---

## ⚠️ Важные правила

1. **НЕ мержить v3.0 в main** пока не готово к релизу
2. **Всегда проверяйте** текущую ветку перед коммитом: `git branch --show-current`
3. **Hotfixes для v2.1** делать в `release/v2.1`
4. **Эксперименты для v3.0** делать в `v3.0-development`
5. **main** трогать только для финальных релизов

---

## 📊 Workflow пример

### Обычный день разработки v3.0:
```bash
# Утро - начало работы
git checkout v3.0-development
git pull origin v3.0-development

# Работа...
# ... пишу код ...

# Коммит
git add .
git commit -m "feat: добавил новую AI модель"
git push origin v3.0-development
```

### Срочный фикс для v2.1:
```bash
# Переключиться на v2.1
git checkout release/v2.1

# Исправить баг
# ... фикс ...

git add .
git commit -m "fix: критический баг в QueryExpander"
git push origin release/v2.1

# Вернуться к v3.0
git checkout v3.0-development
```

---

## 🔍 Проверка текущего состояния

```bash
# Где я сейчас?
git branch --show-current

# Что изменилось?
git status

# История коммитов
git log --oneline --graph --all --decorate
```

---

## 💡 Полезные алиасы

Добавьте в `~/.gitconfig`:

```ini
[alias]
    st = status
    co = checkout
    br = branch
    ci = commit
    current = branch --show-current
```

Теперь можно использовать:
```bash
git current  # показать текущую ветку
git co v3.0-development  # переключиться на v3.0
git co release/v2.1  # переключиться на v2.1
```

---

## 🎉 Текущий статус

✅ **Ветка `release/v2.1` создана** - v2.1.0 защищена  
✅ **Ветка `v3.0-development` создана** - готова к разработке  
✅ **Вы находитесь в `v3.0-development`** - можно начинать!  

**Можете смело экспериментировать - версия 2.1 в безопасности!** 🚀

