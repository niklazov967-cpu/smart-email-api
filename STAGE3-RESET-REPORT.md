# Сброс Stage 3 для компаний без email

## 📋 Задача

Вернуть все компании с сайтом, но без email в очередь на поиск email (Stage 3).

## ✅ Выполнено

### Результаты:

- **13 компаний** с сайтом, но без email найдено
- **13 записей** обновлено
- Все компании добавлены в очередь Stage 3

### Распределение по статусам (до обновления):

**Stage 3 статусы:**
- **11 компаний** - `stage3_status = 'failed'` (Stage 3 провалился)
- **2 компании** - `stage3_status = NULL` (Stage 3 не запускался)

**Stage 2 статусы:**
- **10 компаний** - `stage2_status = 'completed'` (сайт найден в Stage 2)
- **3 компании** - `stage2_status = 'skipped'` (сайт найден в Stage 1)

## 🔧 Что было сделано

### Скрипт: `scripts/reset-stage3-for-companies-without-email.js`

Обновляет параметры для всех компаний с сайтом, но без email:

```javascript
{
  stage3_status: null,         // NULL = готов для обработки
  current_stage: 2,             // Stage 2 завершен, готов для Stage 3
  contacts_json: null,          // Очистить старые контакты
  stage3_raw_data: null,        // Очистить старые данные
  updated_at: new Date().toISOString()
}
```

### Условия выборки:

```sql
WHERE website IS NOT NULL
  AND (email IS NULL OR email = '')
  AND current_stage >= 1
```

## 📊 Примеры обновленных компаний

1. **杭州富阳厂家**
   - Website: `http://hzkcqzjx.chinacrane.net`
   - `stage3_status`: `failed` → `NULL`
   - `current_stage`: `2` → `2`

2. **得可 (DEK Manufacturing)**
   - Website: `https://www.dekmake.com`
   - `stage3_status`: `NULL` → `NULL`
   - `current_stage`: `2` → `2`

3. **博瀚精密制造(苏州)有限公司**
   - Website: `http://www.bohan-pm.com`
   - `stage3_status`: `failed` → `NULL`
   - `current_stage`: `2` → `2`

4. **快速直接**
   - Website: `https://www.rapiddirect.com`
   - `stage3_status`: `NULL` → `NULL`
   - `current_stage`: `2` → `2`

5. **昆山百特罗精密自动化有限公司**
   - Website: `https://baetro-machining.com`
   - `stage3_status`: `failed` → `NULL`
   - `current_stage`: `2` → `2`

...и еще 8 компаний

## 🚀 Использование скрипта

### Dry-run (проверка без изменений):

```bash
node scripts/reset-stage3-for-companies-without-email.js --dry-run
```

### Реальное выполнение:

```bash
node scripts/reset-stage3-for-companies-without-email.js
```

## ✅ Результат

Теперь на главной странице в блоке **"Stage 3: Поиск email"** должно отображаться **13 компаний** готовых для обработки.

Компании можно обработать нажав кнопку **"Обработать"** в Stage 3.

## 🔍 Проверка очереди

SQL запрос для проверки:

```sql
-- Компании готовые для Stage 3
SELECT 
  company_name,
  website,
  stage2_status,
  stage3_status,
  current_stage
FROM pending_companies
WHERE stage3_status IS NULL
  AND current_stage >= 2
  AND website IS NOT NULL
  AND (email IS NULL OR email = '');
```

## 📝 Дата выполнения

**2025-11-30** - 13 компаний добавлены в очередь Stage 3

## 🎯 Итого обработано

### Stage 2 (без сайта):
- 50 компаний добавлено в очередь

### Stage 3 (без email):
- 13 компаний добавлено в очередь

**Всего: 63 компании готовы для дальнейшей обработки!**

