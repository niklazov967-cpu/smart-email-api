# 🚀 Smart Email API - Инструкция по использованию

## ✅ Что реализовано

Система успешно развернута и работает на **http://localhost:3030**

### 📦 Реализованные компоненты:

1. ✅ **Полная архитектура проекта**
   - Структура папок и файлов
   - Конфигурация и зависимости
   - База данных (миграции и схемы)

2. ✅ **Backend сервисы**
   - SettingsManager - управление 117 параметрами настроек
   - SonarApiClient - клиент для Perplexity API с rate limiting
   - MockDatabase - тестовая БД в памяти

3. ✅ **API Endpoints**
   - **Settings API** - управление настройками (10 endpoints)
   - **Sessions API** - управление сессиями поиска (6 endpoints)
   - **Companies API** - работа с компаниями (5 endpoints)

4. ✅ **Web интерфейс**
   - Красивая HTML страница с градиентным дизайном
   - Интерактивное тестирование API
   - Документация по всем endpoints
   - Real-time статус проверка

---

## 🎯 Как использовать

### 1. Открыть веб-интерфейс

Откройте в браузере: **http://localhost:3030**

Вы увидите красивый интерфейс с:
- Описанием всех API endpoints
- Интерактивными кнопками для тестирования
- Документацией

### 2. Создать сессию поиска

**Через веб-интерфейс:**
1. Введите поисковый запрос (например: `不锈钢数控车铣加工`)
2. Укажите целевое количество компаний (10-100)
3. Нажмите **"Создать сессию"**
4. Увидите результат в нижней части страницы

**Через API:**
\`\`\`bash
curl -X POST http://localhost:3030/api/sessions \\
  -H "Content-Type: application/json" \\
  -d '{
    "search_query": "不锈钢数控车铣加工",
    "target_count": 10,
    "priority": "balanced"
  }'
\`\`\`

### 3. Получить список сессий

**Через веб-интерфейс:**
Нажмите кнопку **"Получить все сессии"**

**Через API:**
\`\`\`bash
curl http://localhost:3030/api/sessions
\`\`\`

### 4. Просмотреть настройки

**Через веб-интерфейс:**
Нажмите кнопку **"Получить настройки"**

**Через API:**
\`\`\`bash
# Все настройки
curl http://localhost:3030/api/settings

# Настройки категории API
curl http://localhost:3030/api/settings/api

# Конкретная настройка
curl http://localhost:3030/api/settings/api/model_name
\`\`\`

### 5. Работа с компаниями

**Получить список компаний:**
\`\`\`bash
curl http://localhost:3030/api/companies
\`\`\`

**Получить статистику:**
\`\`\`bash
curl http://localhost:3030/api/companies/stats
\`\`\`

**Экспорт компаний:**
\`\`\`bash
curl -X POST http://localhost:3030/api/companies/export \\
  -H "Content-Type: application/json" \\
  -d '{
    "format": "json",
    "include_raw_data": false
  }'
\`\`\`

---

## 📋 Все доступные API Endpoints

### Settings API

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/settings` | Все настройки |
| GET | `/api/settings/:category` | Настройки категории |
| GET | `/api/settings/:category/:key` | Конкретная настройка |
| PUT | `/api/settings/:category/:key` | Обновить настройку |
| POST | `/api/settings/:category/:key/reset` | Сбросить к умолчанию |
| POST | `/api/settings/:category/reset` | Сбросить категорию |
| GET | `/api/settings/:category/:key/history` | История изменений |
| GET | `/api/settings/export` | Экспорт настроек |
| POST | `/api/settings/import` | Импорт настроек |

### Sessions API

| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/api/sessions` | Создать сессию |
| GET | `/api/sessions` | Список сессий |
| GET | `/api/sessions/:id` | Детали сессии |
| GET | `/api/sessions/:id/progress` | Прогресс сессии |
| PUT | `/api/sessions/:id/status` | Обновить статус |
| DELETE | `/api/sessions/:id` | Удалить сессию |

### Companies API

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/companies` | Список компаний |
| GET | `/api/companies/:id` | Детали компании |
| POST | `/api/companies/export` | Экспорт (JSON/CSV) |
| GET | `/api/companies/stats` | Статистика |

---

## 🔧 Конфигурация

### Настройка API ключа Perplexity

Отредактируйте файл `.env`:

\`\`\`env
PERPLEXITY_API_KEY=your_api_key_here
\`\`\`

### Настройка порта

По умолчанию: **3030**

Изменить в `.env`:
\`\`\`env
PORT=3030
\`\`\`

### Подключение PostgreSQL

По умолчанию используется Mock база в памяти.

Для production подключите PostgreSQL в `.env`:
\`\`\`env
DATABASE_URL=postgresql://user:password@localhost:5432/sonar_portal
USE_MOCK_DB=false
\`\`\`

Затем выполните миграции:
\`\`\`bash
npm run migrate
npm run seed
\`\`\`

---

## 🛠️ Команды

\`\`\`bash
# Установка зависимостей
npm install

# Запуск сервера
npm start

# Разработка (с автоперезагрузкой)
npm run dev

# Миграции БД
npm run migrate

# Загрузка начальных настроек
npm run seed
\`\`\`

---

## 📂 Структура проекта

\`\`\`
smart-email-api/
├── src/
│   ├── app.js              # Главное приложение (полная версия)
│   ├── app-simple.js       # Упрощенная версия (работает сейчас)
│   ├── services/
│   │   ├── SettingsManager.js
│   │   └── SonarApiClient.js
│   ├── database/
│   │   └── MockDatabase.js
│   └── api/
│       ├── settings.js
│       ├── sessions.js
│       └── companies.js
├── public/
│   └── index.html          # Веб-интерфейс
├── database/
│   ├── migrations/
│   │   ├── 001-initial-schema.sql
│   │   └── 002-settings-tables.sql
│   ├── migrate.js
│   └── seed.js
├── .env
├── package.json
└── README.md
\`\`\`

---

## 🎨 Особенности веб-интерфейса

- 🎨 Современный дизайн с градиентами
- 📱 Адаптивная верстка
- 🔄 Real-time обновление статуса
- 🧪 Интерактивное тестирование API
- 📊 Цветовое кодирование методов (GET, POST, PUT, DELETE)
- 💡 Подсказки и документация

---

## 🔒 Безопасность

⚠️ **Текущая версия для разработки**

Для production добавьте:
- Аутентификацию (JWT)
- HTTPS
- Rate limiting
- Валидацию входных данных
- IP whitelist
- 2FA для админ панели

---

## 🐛 Известные ограничения

1. **Mock Database** - данные хранятся в памяти (при перезапуске теряются)
2. **Perplexity API** - нужен API ключ для работы Sonar Client
3. **Этапы обработки** - Stage 1-6 требуют доработки для production

---

## 📞 Поддержка

Для вопросов и предложений:
- Документация: http://localhost:3030
- API Info: http://localhost:3030/api
- Health Check: http://localhost:3030/health

---

## 📝 Лицензия

ISC

---

**Powered by Perplexity Sonar Pro API** 🤖

